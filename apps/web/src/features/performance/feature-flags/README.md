# EliteFlow Performance Foundation (Task 1.4)

See [PERFORMANCE_FLAGS.md](./PERFORMANCE_FLAGS.md) for flag table and usage.

## Phase 1

1. Typed `PERFORMANCE_*` flags (all default OFF)
2. Baseline + component + React Query + bundle audits
3. Reusable utilities (`useStableCallback`, memo helpers, query overlay helper, optional render profiler)

Phase 1 does **not** change product UX or apply optimizations automatically.

## Phase 2

Opt-in application of flags on AI Assistant, AI Documents, and Reports. See [PERFORMANCE_PHASE2_OPTIMIZATION_REPORT.md](./PERFORMANCE_PHASE2_OPTIMIZATION_REPORT.md).

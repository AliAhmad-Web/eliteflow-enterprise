/**
 * Temporary: hide customer payment-proof screenshot upload.
 * Set to `true` to restore Transaction ID + screenshot (File Manager) submit.
 * Do not delete the upload UI in invoice-pay-panel — it is gated by this flag.
 */
export const REQUIRE_PAYMENT_PROOF_UPLOAD = false;
